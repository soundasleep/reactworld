<!DOCTYPE html>
<html>
  <head>
    <title>Reactworld</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%# Include everything (incl React) in one giant pack %>
    <%= javascript_pack_tag 'application' %>
    <%= stylesheet_pack_tag 'application' %>

    <%#
      Split out common components, include things as necessary. Requires webpacker v4+
      (still in -pre) and an intimate knowledge of how webpacker packs work.
    %>
    <%#= javascript_packs_with_chunks_tag 'application', 'just_hello_react' %>
    <%#= stylesheet_packs_with_chunks_tag 'application', 'just_hello_react' %>
  </head>

  <body>
    <div class="navigation">
      <% if current_user %>
        <%= link_to "Your games", root_path %>
        <%= link_to "Sign out", signout_path %>
        <%= current_user.name %>
      <% else %>
        <%= link_to "Sign in with Google", "/auth/google_login" %>
      <% end %>
    </div>

    <% if flash[:errors].present? %>
      <div class="error">
        <%= flash[:errors] %>
      </div>
    <% end %>

    <% if flash[:notices].present? %>
      <div class="notice">
        <%= flash[:notices] %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
